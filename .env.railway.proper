# Database Configuration - MUST be updated with ACTUAL values
DATABASE_URL=postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}
DIRECT_URL=${DATABASE_URL}

# JWT Configuration - CRITICAL for authentication
JWT_SECRET=use-a-long-random-strong-secret-key-here
JWT_EXPIRY=24h

# CORS Configuration - Must match your Vercel frontend URL exactly
CORS_ORIGIN=https://airbnb-frontend-i8p5-git-main-karoliskalinauskas1s-projects.vercel.app
FRONTEND_URL=https://airbnb-frontend-i8p5-git-main-karoliskalinauskas1s-projects.vercel.app

# Supabase Configuration - Must use actual values
SUPABASE_URL=https://kyxmgifuawpoiocyuexh.supabase.co
SUPABASE_ANON_KEY=your-actual-supabase-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-actual-supabase-service-role-key-here

# Server Configuration
PORT=3000
NODE_ENV=production

# Security
BCRYPT_SALT_ROUNDS=12
RATE_LIMIT_WINDOW=15m
RATE_LIMIT_MAX=100

# Connection Pool Settings
DATABASE_CONNECTION_LIMIT=5
DATABASE_IDLE_TIMEOUT=10000
DATABASE_CONNECTION_TIMEOUT=30000

# Debug Options (set to 'true' to enable, 'false' to disable)
ENABLE_REQUEST_LOGGING=true
ENABLE_ERROR_STACK_TRACES=false

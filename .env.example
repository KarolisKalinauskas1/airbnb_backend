# Supabase Configuration
# URL of your Supabase project (required)
SUPABASE_URL=https://your-project.supabase.co

# Public/anonymous API key - the safest one to use for auth middleware (required)
# In Supabase dashboard: Project Settings > API > Project API keys > anon public
SUPABASE_ANON_KEY=your_anon_key_here

# Alternative name for the anon key (if your code uses this name instead)
SUPABASE_KEY=your_anon_key_here

# Service role key - for admin operations only, has full database access (optional)
# In Supabase dashboard: Project Settings > API > Project API keys > service_role
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Stripe API Keys
STRIPE_SECRET_KEY=your_stripe_secret_key_here

# Frontend URL (for redirects)
FRONTEND_URL=http://localhost:5173
